# feedsystem_video_go 项目导览

## 1. 项目要解决的核心问题

`feedsystem_video_go` 是一个短视频 Feed 系统，用来支撑类似短视频社区的核心业务链路：用户账号、视频发布、推荐/关注 Feed、点赞、评论、关注、私信和通知。

从架构角度看，它重点解决的不只是“上传和播放视频”，而是一个短视频应用在工程上会遇到的典型问题：

- 如何组织账号、视频、社交关系、互动数据和 Feed 流。
- 如何通过 Redis 缓存提升视频详情、Feed 时间线、热榜等高频读取场景的性能。
- 如何通过 RabbitMQ 把点赞、评论、关注、热度更新、通知等事件异步化，降低接口同步写入压力。
- 如何支持大文件分片上传、断点续传和 MD5 校验。
- 如何用 SSE 实现点赞、评论、关注、提及等通知的实时推送。
- 如何用 Docker Compose 快速拉起 MySQL、Redis、RabbitMQ、后端 API、Worker 和前端。

因此，这个项目更像是一个面向学习和实践的“短视频 Feed 后端 + 前端完整样例”，核心价值在于展示短视频系统的业务建模、异步事件处理、缓存策略和前后端协作方式。

## 2. 核心技术栈

### 后端

- 语言：Go 1.24.5
- Web 框架：Gin
- ORM：GORM
- 数据库：MySQL
- 缓存与时间线：Redis，使用 `go-redis/v9`
- 消息队列：RabbitMQ，使用 `amqp091-go`
- 鉴权：JWT，使用 `github.com/golang-jwt/jwt/v5`
- 配置：YAML 配置文件 + 环境变量，使用 `gopkg.in/yaml.v3` 和 `godotenv`
- 密码安全：`golang.org/x/crypto`
- 可观测性：pprof、健康检查
- 异步处理：独立 Worker 进程消费 RabbitMQ 事件

### 前端

- 框架：Vue 3
- 构建工具：Vite，当前通过 `rolldown-vite` 覆盖 Vite 包
- 语言：TypeScript
- 路由：Vue Router
- 状态管理：Pinia
- 文件 MD5：spark-md5，用于视频分片上传校验
- API 协作：前端通过 `/api` 代理访问后端，401 时支持 refresh token 自动刷新并重试

### 基础设施

- Docker Compose：一键启动 MySQL、Redis、RabbitMQ、后端 API、Worker 和前端
- Nginx：Docker 环境中托管前端构建产物，并反向代理 API
- RabbitMQ 管理台：用于查看队列、交换机和消费情况

## 3. 顶层目录结构

```text
.
├── backend/
├── frontend/
├── picture/
├── test/
├── README.md
├── feedsystem_video_go项目设计.md
├── start.sh
└── docker-compose.yml
```

说明：下面主要说明业务源码和项目理解相关目录；测试目录、隐藏目录和纯配置文件不展开。

## 4. 主要目录说明

### `backend/`

后端服务目录，是整个系统的业务核心。它包含 API 服务、Worker 服务、领域模块、数据库访问、缓存、消息队列、鉴权、限流和可观测性能力。

主要结构：

```text
backend/
├── cmd/
├── configs/
└── internal/
```

- `cmd/`：后端进程入口。
  - `cmd/main.go` 启动 HTTP API 服务。
  - `cmd/worker/main.go` 启动异步 Worker，用于消费 RabbitMQ 中的点赞、评论、关注、通知、热度等事件。
- `configs/`：后端运行配置目录，包含本地、Docker 等不同环境的配置文件。
- `internal/`：后端核心业务代码，采用 Go 项目常见的 internal 包组织方式，避免被外部项目直接引用。

`backend/internal/` 的核心模块：

- `account/`：账号模块，处理注册、登录、用户资料、头像、改名、改密、主页统计等。
- `auth/`：JWT 相关逻辑，负责 token 生成、解析和校验。
- `db/`：数据库初始化和连接管理。
- `http/`：HTTP 路由装配，把各业务 handler 注册到 Gin 路由。
- `feed/`：Feed 流模块，包含推荐流、关注流、热榜、点赞榜、话题流和游标分页等逻辑。
- `video/`：视频主业务模块，包含视频发布、上传、分片上传、封面、详情、点赞、评论、话题标签、热度缓存等。
- `social/`：关注关系模块，处理关注、取关、粉丝列表、关注列表和计数。
- `message/`：私信模块，处理会话消息发送和最近消息列表。
- `worker/`：异步任务消费者模块，处理点赞落库、评论落库、关注事件、通知推送、热度更新、Outbox 等后台任务。
- `middleware/`：通用中间件和基础设施封装。
  - `jwt/`：HTTP 鉴权中间件。
  - `ratelimit/`：限流逻辑。
  - `redis/`：Redis 客户端、缓存和 ZSet 操作封装。
  - `rabbitmq/`：RabbitMQ 连接、交换机、队列、死信队列和各类业务事件发布封装。
- `observability/`：pprof 等可观测性支持。
- `apierror/`：统一 API 错误定义。
- `config/`：配置加载逻辑。

### `frontend/`

前端应用目录，提供短视频 Feed 的浏览、播放、发布、账号、用户主页、私信等界面。它是一个 Vue 3 + TypeScript + Vite 应用。

主要结构：

```text
frontend/
├── src/
├── index.html
└── package.json
```

`frontend/src/` 的核心模块：

- `main.ts`：前端应用入口，初始化 Vue、Pinia 和路由。
- `App.vue`：根组件。
- `style.css`：全局样式。
- `api/`：前端 API 调用层，按账号、视频、Feed、点赞、评论、关注、私信等业务拆分；`client.ts` 负责统一请求、token 注入和刷新重试。
- `router/`：前端路由定义。
- `stores/`：Pinia 状态管理，包含登录态、社交关系、toast 等状态。
- `views/`：页面级组件。
  - `FeedView.vue`：推荐/关注/点赞榜等沉浸式 Feed 页面。
  - `HotView.vue`：热榜页面。
  - `VideoView.vue`：视频发布页面，包含分片上传链路。
  - `VideoDetailView.vue`：视频详情、播放、点赞和评论。
  - `AccountView.vue`、`RegisterView.vue`、`ChangePasswordView.vue`、`SettingsView.vue`：账号相关页面。
  - `UserProfileView.vue`：用户主页、作品、粉丝关注与私信入口。
  - `MessageView.vue`：私信联系人与会话页面。
- `components/`：可复用 UI 组件，例如应用外壳、视频卡片、评论抽屉、头像、toast、JSON 展示等。
- `composables/`：复用交互逻辑，例如视频 Feed 加载、播放器控制、点赞关注操作。
- `types/`：前端类型声明。
- `utils/`：通用工具，例如 JWT 解析和错误上报。

### `picture/`

项目配图和设计说明图目录。这里包含整体架构、整体流程、表关系、用户表、视频表、点赞表、评论表、关注表、Feed 缓存/分页流程、点赞评论异步落库和热度更新等图示。

这个目录对理解系统设计很有帮助，适合配合 `feedsystem_video_go项目设计.md` 一起阅读。

### `test/`

测试相关目录。根据本次梳理要求，本文不展开测试目录。

## 5. 启动入口与程序生命周期

这个项目是前后端分离、API 与 Worker 分进程运行的结构，因此它不是只有一个入口文件，而是有三个主要入口：

- 后端 API 入口：`backend/cmd/main.go`
- 后端 Worker 入口：`backend/cmd/worker/main.go`
- 前端入口：`frontend/src/main.ts`

`start.sh` 和 `docker-compose.yml` 属于启动编排层，用来拉起依赖服务和应用进程；真正执行业务代码的入口是上面三个文件。

### 后端 API 启动流程

`backend/cmd/main.go` 是 HTTP API 服务的启动入口。它启动后主要执行下面这些初始化工作：

1. 调用 `godotenv.Load()` 加载 `.env`，用于本地开发环境变量。
2. 读取 `CONFIG_PATH`，如果没有设置，则默认使用 `configs/config.yaml`。
3. 调用 `config.LoadLocalDev(configPath)` 加载 YAML 配置和默认本地配置。
4. 调用 `db.NewDB(cfg.Database)` 连接 MySQL。
5. 调用 `db.AutoMigrate(sqlDB)` 自动迁移业务表结构。
6. 调用 `rediscache.NewFromEnv(&cfg.Redis)` 初始化 Redis，并通过 `Ping` 检查可用性；Redis 不可用时降级为无缓存模式。
7. 调用 `rabbitmq.NewRabbitMQ(&cfg.RabbitMQ)` 初始化 RabbitMQ；RabbitMQ 不可用时降级为无 MQ 模式。
8. 调用 `observability.NewPprofServer(...)` 启动 API pprof。
9. 调用 `apphttp.SetRouter(sqlDB, cache, rmq)` 装配 Gin 路由、业务模块、中间件和部分后台协程。
10. 调用 `r.Run(":" + port)` 开始监听 HTTP 请求。

可以把 API 进程理解成系统的“同步请求入口”：它负责接收 HTTP 请求、完成鉴权和参数校验、调用业务 Service，并把适合异步处理的事件投递到 RabbitMQ。

### 路由与业务对象装配

`backend/internal/http/router.go` 中的 `SetRouter` 是 API 服务最重要的装配函数。它主要做三类事情：

- 创建基础 Gin Engine，注册 `/healthz` 健康检查和 `/static` 静态文件访问。
- 创建限流器、JWT 中间件、各业务模块的 `Repository -> Service -> Handler`。
- 注册账号、视频、点赞、评论、关注、Feed、私信、通知等 HTTP 路由。

典型的模块装配方式如下：

```text
GORM DB / Redis / RabbitMQ
  |
  v
Repository
  |
  v
Service
  |
  v
Handler
  |
  v
Gin Router
```

例如点赞模块在 `SetRouter` 中会创建：

- `video.NewLikeRepository(db)`
- `video.NewLikeService(likeRepository, videoRepository, cache, likeMQ, popularityMQ)`
- `video.NewLikeHandler(likeService)`
- `protectedLikeGroup.POST("/like", likeLimiter, likeHandler.Like)`

### 后端 Worker 启动流程

`backend/cmd/worker/main.go` 是异步 Worker 进程入口。它启动后主要执行下面这些初始化工作：

1. 加载 `.env` 和配置文件。
2. 使用 `connectWithRetry` 带重试连接 MySQL。
3. 初始化 Redis，用于热度缓存更新。
4. 使用 `connectWithRetry` 带重试连接 RabbitMQ。
5. 声明 RabbitMQ 的 exchange、queue 和 binding。
6. 创建 `SocialRepository`、`VideoRepository`、`LikeRepository`、`CommentRepository`。
7. 创建可响应 `SIGINT`、`SIGTERM` 的 context，用于优雅退出。
8. 启动 Worker pprof。
9. 通过 goroutine 启动多个消费者：
   - `SocialWorker`
   - `LikeWorker`
   - `CommentWorker`
   - `PopularityWorker`
10. 阻塞等待退出信号，退出前等待正在处理的消息完成。

可以把 Worker 进程理解成系统的“异步事件处理器”：它负责消费 RabbitMQ 事件，完成异步落库、计数更新、热度缓存更新等后台工作。

### 前端启动流程

`frontend/src/main.ts` 是前端入口。它的启动逻辑比较轻：

1. 调用 `createApp(App)` 创建 Vue 应用。
2. 注册 Pinia 状态管理。
3. 注册 Vue Router。
4. 注册全局错误处理器 `app.config.errorHandler`。
5. 调用 `app.mount("#app")` 挂载到页面。

前端路由定义在 `frontend/src/router/index.ts`，其中带 `meta.requiresAuth` 的页面会在 `router.beforeEach` 中检查登录态，未登录时跳转到 `/account`。

## 6. 核心调用链：用户点赞视频

以 `POST /like/like` 为例，这条链路能代表项目最核心的执行模式：前端发起操作，API 完成同步校验和事件投递，Worker 异步落库和更新热度。

### 执行时序图

```text
用户点击点赞
  |
  v
frontend/src/composables/useLikeFollow.ts
toggleLike(item)
  |
  v
frontend/src/api/like.ts
like(videoId)
  |
  v
frontend/src/api/client.ts
postJson("/like/like", { video_id }, { authRequired: true })
  |
  v
HTTP POST /api/like/like
Authorization: Bearer <token>
  |
  v
Gin Router
protectedLikeGroup.POST("/like", likeLimiter, likeHandler.Like)
  |
  v
jwt.JWTAuth
解析 token -> 校验 Redis/DB 中保存的 token -> 写入 accountID 到 gin.Context
  |
  v
likeLimiter
按账号维度做点赞写入限流
  |
  v
LikeHandler.Like
解析 JSON -> 校验 video_id -> jwt.GetAccountID(c)
  |
  v
LikeService.Like
校验视频存在 -> 查询是否已点赞
  |
  +--------------------------------------------------+
  | RabbitMQ 可用                                    |
  |                                                  |
  | 1. likeMQ.Like                                   |
  |    投递 like.like 事件到 like.events              |
  |                                                  |
  | 2. popularityMQ.Update                           |
  |    投递 video.popularity.update 热度 +1 事件      |
  |                                                  |
  | 3. API 返回 200                                  |
  +--------------------------------------------------+
  |
  +--------------------------------------------------+
  | RabbitMQ 不可用或投递失败                         |
  |                                                  |
  | 直接开启 MySQL 事务：                              |
  | 创建 likes 记录                                   |
  | videos.likes_count + 1                            |
  | videos.popularity + 1                             |
  |                                                  |
  | 如果热度 MQ 失败，则直接更新 Redis 热度缓存          |
  +--------------------------------------------------+

异步侧：

RabbitMQ like.events
  |
  v
LikeWorker.Run
  |
  v
LikeWorker.process
解析 rabbitmq.LikeEvent
  |
  v
LikeWorker.applyLike
检查视频存在
  |
  v
LikeRepository.LikeIgnoreDuplicate
写入 likes 表，重复点赞会被幂等忽略
  |
  v
VideoRepository.ChangeLikesCount
更新 videos.likes_count
  |
  v
VideoRepository.ChangePopularity
更新 videos.popularity

RabbitMQ video.popularity.events
  |
  v
PopularityWorker.Run
  |
  v
PopularityWorker.process
解析 rabbitmq.PopularityEvent
  |
  v
video.UpdatePopularityCache
更新 Redis 热度榜缓存

RabbitMQ notification.like
  |
  v
NotificationWorker.process
查询视频作者
  |
  v
写入 notifications 表
  |
  v
SSEHub.Push
如果作者在线，通过 SSE 实时推送通知
```

### 关键函数

- 前端点赞入口：`frontend/src/composables/useLikeFollow.ts` 中的 `toggleLike`
- 前端 API 调用：`frontend/src/api/like.ts` 中的 `like`
- 前端统一请求：`frontend/src/api/client.ts` 中的 `postJson`
- 后端路由注册：`backend/internal/http/router.go` 中的 `SetRouter`
- JWT 鉴权：`backend/internal/middleware/jwt/jwt.go` 中的 `JWTAuth`
- 点赞 Handler：`backend/internal/video/like_handler.go` 中的 `LikeHandler.Like`
- 点赞 Service：`backend/internal/video/like_service.go` 中的 `LikeService.Like`
- 点赞事件发布：`backend/internal/middleware/rabbitmq/likeMQ.go` 中的 `LikeMQ.Like`
- 热度事件发布：`backend/internal/middleware/rabbitmq/popularityMQ.go` 中的 `PopularityMQ.Update`
- 点赞 Worker：`backend/internal/worker/likeworker.go` 中的 `LikeWorker.Run`、`LikeWorker.process`、`LikeWorker.applyLike`
- 热度 Worker：`backend/internal/worker/popularityworker.go` 中的 `PopularityWorker.Run`、`PopularityWorker.process`
- 通知 Worker：`backend/internal/worker/notificationworker.go` 中的 `NotificationWorker.process`
- SSE 推送：`backend/internal/worker/ssehub.go` 中的 `SSEHub.Push`

### 这条链路体现的架构特点

- API 层尽量保持轻量，完成鉴权、限流、校验和事件投递后快速返回。
- 写 MySQL 和更新热度可以异步化，降低用户请求的同步等待时间。
- RabbitMQ 不可用时，Service 中有同步降级逻辑，保证核心点赞动作仍然可以完成。
- Worker 侧通过幂等写入和重试处理，降低重复消息和短暂失败带来的影响。
- Redis 主要承担缓存、热榜和高频读取加速职责。
- SSE 通知由 RabbitMQ 事件驱动，在线用户可以实时收到点赞、评论、关注等通知。

一句话总结：这个项目的 API 进程负责“接请求、校验、编排、投递事件”，Worker 进程负责“消费事件、异步落库、更新热度和通知”。

## 7. 关键文件说明

- `README.md`：项目总览、功能列表、启动方式、接口清单、环境变量和运维说明。
- `feedsystem_video_go项目设计.md`：更详细的项目设计文档，适合在读完本导览后继续深入。
- `start.sh`：本地一键启动脚本，可启动依赖服务、后端 API、Worker 和前端。
- `docker-compose.yml`：容器编排文件，用于拉起 MySQL、Redis、RabbitMQ、后端、Worker 和前端。
- `backend/cmd/main.go`：后端 API 服务入口。
- `backend/cmd/worker/main.go`：后端异步 Worker 入口。
- `backend/internal/http/router.go`：后端路由和业务对象装配中心。
- `frontend/src/main.ts`：前端应用入口。

## 8. 建议阅读顺序

如果你想快速熟悉这个项目，可以按下面顺序阅读：

1. 先读 `README.md`，建立业务能力全景。
2. 再看 `backend/cmd/main.go` 和 `backend/internal/http/router.go`，理解 API 服务如何启动和注册路由。
3. 选择一个业务模块纵向阅读，例如 `video/` 或 `feed/`，按 `handler -> service -> repo -> entity` 的顺序看。
4. 阅读 `backend/internal/middleware/redis/` 和 `backend/internal/middleware/rabbitmq/`，理解缓存和异步事件如何支撑高频互动。
5. 阅读 `backend/cmd/worker/main.go` 和 `backend/internal/worker/`，理解异步落库、通知和热度更新链路。
6. 最后看 `frontend/src/api/`、`frontend/src/views/FeedView.vue`、`frontend/src/views/VideoView.vue`，把前端调用和后端接口串起来。

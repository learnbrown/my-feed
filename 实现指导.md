我读完两份文档后的判断是：原项目已经是“毕业形态”，你不能一上来照着全量实现。正确路线是先做一个能跑通业务闭环的单体，再逐步把性能问题“逼出来”，最后用 Redis、MQ、Worker、SSE 去解决它。

## 1. 全局架构演进图

```text
V0.1 单体骨架
Gin + GORM + MySQL
账号注册 / 登录 / JWT / 基础分层

        ↓

V0.2 视频 MVP
本地上传 + 视频发布 + 最新 Feed + 视频详情

        ↓

V0.3 互动闭环
点赞 / 评论 / 关注 / 用户主页 / 基础私信
全部同步写 MySQL

        ↓

V1.0 高并发读优化
Redis 缓存 / Feed 时间线 / 热榜 / 限流 / 分片上传

        ↓

V1.5 事件驱动增强
RabbitMQ + Worker + Outbox + 异步点赞评论关注 + SSE 通知

        ↓ 可选

V2.0 微服务体验版
gRPC 拆分账号 / 视频 / 互动 / Feed 服务
```

你简历上真正能讲出东西的是 V1.0 和 V1.5，但你必须先把 V0.1 到 V0.3 写扎实。否则后面只是堆中间件，面试官一问业务一致性，你就会露馅。

## 2. 阶段性拆解指南

## V0.1：单体应用骨架 + 账号系统

### 阶段目标

先把后端项目跑起来，完成最小可用的账号系统：

- 用户注册
- 用户登录
- JWT 鉴权
- 当前用户信息查询
- 修改密码 / 登出可以先做简版

这一阶段不要碰 Redis、RabbitMQ、分片上传。你现在的目标是建立工程基本功。

### 技术栈引入

- `Gin`：路由、中间件、参数绑定。
- `GORM`：快速建模 MySQL 表。
- `MySQL`：核心业务数据持久化。
- `JWT`：无状态登录凭证。
- `bcrypt`：密码哈希。

一开始不用 Redis，因为你的用户量还不存在，先把 token 生成、解析、鉴权中间件写清楚。

### 建表思路

```sql
accounts
- id bigint primary key
- username varchar(64) unique not null
- password_hash varchar(255) not null
- avatar_url varchar(255)
- bio varchar(255)
- token text
- refresh_token text
- created_at datetime
- updated_at datetime
```

V0.1 可以先保留 `token` 字段，用于服务端主动撤销 token。等 V1.0 再把 token 缓存放进 Redis。

### 开发顺序建议

1. 初始化项目结构：`cmd/main.go`、`internal/config`、`internal/db`、`internal/account`、`internal/middleware/jwt`。
2. 写数据库连接和 `AutoMigrate`。
3. 写 `Account` model。
4. 写 `POST /account/register`。
5. 写 `POST /account/login`，返回 `token`。
6. 写 JWT 生成和解析逻辑。
7. 写 `JWTAuth` 中间件。
8. 写 `GET /account/me` 或 `POST /account/findByID`。
9. 写 `POST /account/logout`，清空数据库 token。
10. 写统一响应结构和统一错误处理。

### 难点预警

- 密码绝不能明文入库，必须用 bcrypt。
- JWT 过期时间不要太长，后面再加 refresh token。
- Gin 的 `Context` 里保存 `account_id` 时要统一 key，别每个模块自己乱取。
- 这一阶段别过度设计 repository/service 目录，但至少要做到 `handler -> service -> repository` 三层分明。

---

## V0.2：视频发布 MVP + 基础 Feed

### 阶段目标

跑通短视频系统最核心链路：

```text
登录用户上传视频文件
  -> 上传封面文件
  -> 发布视频元数据
  -> 首页最新 Feed 能刷到视频
  -> 点进视频详情能播放
  -> 能按作者查看作品
  -> 可选：能按 #话题 查看视频
```

这一版只做本地文件存储，不接 OSS，不做分片上传，不做 Redis 缓存，不做 MQ。先把“文件上传 + 数据入库 + Feed 查询”这条主链路写稳。

### 技术栈引入

- `multipart/form-data`：处理视频和封面上传。上传接口和普通 JSON 接口不同，不能用 `ShouldBindJSON`，而是从 `FormFile` 读取文件。
- 本地磁盘存储：先写到 `.run/uploads/videos/` 和 `.run/uploads/covers/`，本地开发最直接，方便你理解 URL 和磁盘路径的区别。
- MySQL / SQLite：保存视频元信息、话题、视频和话题的关系。文件本身不进数据库，数据库只保存可访问 URL。
- Gin 静态文件服务：通过 `/static/*filepath` 暴露本地上传目录，让浏览器能访问视频和封面。

暂时不用对象存储。你现在要先学会上传链路、元数据建模、Feed 分页和静态资源访问，不要被 OSS 签名、回调、权限、CDN 这些后续问题打散注意力。

### 模块建议

建议新增两个业务模块：

```text
internal/video/
  model.go        // Video / Tag / VideoTag 模型
  repository.go   // 视频、标签相关 DB 操作
  service.go      // 上传、发布、详情、作者列表、标签提取
  handler.go      // /video 路由处理

internal/feed/
  service.go      // Feed 查询逻辑
  handler.go      // /feed 路由处理
```

V0.2 可以让 `feed` 直接依赖 `video.Repository` 查询视频。后面 V1.0 引入 Redis 后，再把 Feed 的冷热数据、时间线缓存拆得更细。

### 建表思路

#### `videos`：视频主表

这张表保存一个视频的核心元信息。注意：视频文件本体不放数据库，数据库只保存 `play_url` 和 `cover_url`。

```sql
videos
- id bigint primary key
- author_id bigint not null
- title varchar(128) not null
- description text
- play_url varchar(255) not null
- cover_url varchar(255)
- likes_count bigint default 0
- comments_count bigint default 0
- popularity bigint default 0
- status tinyint default 1
- created_at datetime
- updated_at datetime
- deleted_at datetime null
```

字段含义：

| 字段 | 含义 | V0.2 使用方式 |
| --- | --- | --- |
| `id` | 视频 ID，主键 | 详情查询、Feed 返回、后续点赞评论都靠它关联 |
| `author_id` | 作者账号 ID | 从 JWT 中取当前登录用户 ID，不让前端自己传作者 ID |
| `title` | 视频标题 | 发布时必填，限制长度，比如 1-128 |
| `description` | 视频描述 | 可选；后续可以从这里提取 `#话题` |
| `play_url` | 视频播放 URL | 上传成功后返回给前端，发布时写入数据库 |
| `cover_url` | 封面图片 URL | 上传封面后返回；可以允许为空，前端用默认封面 |
| `likes_count` | 点赞数冗余字段 | V0.2 固定为 0，V0.3 点赞时维护 |
| `comments_count` | 评论数冗余字段 | V0.2 固定为 0，V0.3 评论时维护 |
| `popularity` | 热度分 | V0.2 固定为 0，V1.0 热榜会用 |
| `status` | 视频状态 | `1` 表示正常；以后可扩展为审核中、删除、封禁 |
| `created_at` | 创建时间 | Feed 最新流排序依据 |
| `updated_at` | 更新时间 | 后续编辑视频信息时使用 |
| `deleted_at` | 软删除时间 | 使用 `gorm.Model` 时自动拥有；V0.2 可以先不用 |

建议索引：

```sql
index idx_videos_author_created_at(author_id, created_at)
index idx_videos_created_at(created_at)
```

Go 模型大致方向：

```go
type Video struct {
    gorm.Model
    AuthorID      uint   `gorm:"index;not null" json:"author_id"`
    Title         string `gorm:"size:128;not null" json:"title"`
    Description   string `gorm:"type:text" json:"description"`
    PlayURL       string `gorm:"size:255;not null" json:"play_url"`
    CoverURL      string `gorm:"size:255" json:"cover_url"`
    LikesCount    int64  `gorm:"default:0" json:"likes_count"`
    CommentsCount int64  `gorm:"default:0" json:"comments_count"`
    Popularity    int64  `gorm:"default:0" json:"popularity"`
    Status        int    `gorm:"default:1" json:"status"`
}
```

#### `tags`：话题表

这张表保存系统里出现过的话题，例如 `Go`、`后端`、`旅行`。同一个话题只存一行。

```sql
tags
- id bigint primary key
- name varchar(64) unique not null
- created_at datetime
- updated_at datetime
```

字段含义：

| 字段 | 含义 | V0.2 使用方式 |
| --- | --- | --- |
| `id` | 话题 ID | 被 `video_tags.tag_id` 引用 |
| `name` | 话题名称 | 去掉 `#` 后的内容，比如 `#Go` 存为 `Go` |
| `created_at` | 创建时间 | 第一次出现该话题时写入 |
| `updated_at` | 更新时间 | 基本不用，GORM 自动维护 |

建议索引：

```sql
unique index uk_tags_name(name)
```

#### `video_tags`：视频和话题关系表

这是多对多关系表。一个视频可以有多个话题，一个话题也可以对应多个视频。

```sql
video_tags
- id bigint primary key
- video_id bigint not null
- tag_id bigint not null
- created_at datetime
- unique(video_id, tag_id)
```

字段含义：

| 字段 | 含义 | V0.2 使用方式 |
| --- | --- | --- |
| `id` | 关系 ID | 主键 |
| `video_id` | 视频 ID | 指向 `videos.id` |
| `tag_id` | 话题 ID | 指向 `tags.id` |
| `created_at` | 关系创建时间 | 发布视频并提取话题时写入 |

为什么需要关系表：不要在 `videos` 表里用字符串存 `"Go,后端,旅行"`。那样后面按话题查询、去重、统计都会很难。关系表是标准做法。

V0.2 如果你感觉压力大，可以先只实现 `videos`。但建议至少把 `tags/video_tags` 的模型建出来，发布接口后半段再补话题提取。

### 路由设计

#### `POST /video/uploadVideo`

功能：上传视频文件，返回可访问的视频 URL。

- 鉴权：需要登录。
- 请求类型：`multipart/form-data`。
- 表单字段：`file`。
- 文件限制：V0.2 只允许 `.mp4`，大小可以先限制 200MB。
- 落盘位置示例：`.run/uploads/videos/<accountID>/<yyyyMMdd>/<uniqueName>.mp4`。
- 返回示例：

```json
{
  "url": "/static/uploads/videos/1/20260618/xxx.mp4",
  "play_url": "/static/uploads/videos/1/20260618/xxx.mp4"
}
```

注意：这个接口只负责“上传文件”，不创建 `videos` 表记录。

#### `POST /video/uploadCover`

功能：上传封面图片，返回可访问的封面 URL。

- 鉴权：需要登录。
- 请求类型：`multipart/form-data`。
- 表单字段：`file`。
- 文件限制：`.jpg/.jpeg/.png/.webp`，大小可以先限制 10MB。
- 落盘位置示例：`.run/uploads/covers/<accountID>/<yyyyMMdd>/<uniqueName>.jpg`。
- 返回示例：

```json
{
  "url": "/static/uploads/covers/1/20260618/xxx.jpg",
  "cover_url": "/static/uploads/covers/1/20260618/xxx.jpg"
}
```

#### `POST /video/publish`

功能：发布视频元数据，真正创建 `videos` 表记录。

- 鉴权：需要登录。
- 请求类型：`application/json`。
- 请求体：

```json
{
  "title": "我的第一个 Go 视频",
  "description": "从零写 Feed 系统 #Go #后端",
  "play_url": "/static/uploads/videos/1/20260618/xxx.mp4",
  "cover_url": "/static/uploads/covers/1/20260618/xxx.jpg"
}
```

- 服务端行为：
  - 从 JWT 上下文取 `author_id`。
  - 校验 `title` 和 `play_url`。
  - 创建 `videos` 记录。
  - 从 `title + description` 提取 `#话题`，写入 `tags` 和 `video_tags`。

返回示例：

```json
{
  "video": {
    "id": 1,
    "author_id": 1,
    "title": "我的第一个 Go 视频",
    "description": "从零写 Feed 系统 #Go #后端",
    "play_url": "/static/uploads/videos/1/20260618/xxx.mp4",
    "cover_url": "/static/uploads/covers/1/20260618/xxx.jpg",
    "likes_count": 0,
    "comments_count": 0,
    "popularity": 0
  }
}
```

#### `POST /video/listByAuthorID`

功能：查询某个作者发布的视频列表。

- 鉴权：可先不强制登录；后面需要显示“是否已关注/是否已点赞”时再做软鉴权。
- 请求体：

```json
{
  "author_id": 1,
  "limit": 20,
  "latest_time": 0
}
```

- 排序：按 `created_at desc`。
- 用途：用户主页作品列表。

#### `POST /video/getDetail`

功能：查询单个视频详情。

- 鉴权：V0.2 可以不强制登录。
- 请求体：

```json
{
  "id": 1
}
```

- 返回：视频基本信息。
- V1.0 再扩展：作者信息、是否点赞、是否关注、评论预览、Redis 缓存。

#### `POST /feed/listLatest`

功能：首页最新视频流。

- 鉴权：V0.2 可以不强制登录。
- 请求体：

```json
{
  "limit": 20,
  "latest_time": 0
}
```

- `latest_time = 0` 表示第一页。
- 返回：

```json
{
  "videos": [],
  "next_time": 1718690000000,
  "has_more": true
}
```

`next_time` 是本页最后一个视频的创建时间毫秒数，下一页请求时把它作为 `latest_time` 传回来。

#### `POST /feed/listByTag`

功能：按话题查询视频。

- 鉴权：V0.2 可以不强制登录。
- 请求体：

```json
{
  "tag_name": "Go",
  "limit": 20,
  "latest_time": 0
}
```

- 查询方式：`tags -> video_tags -> videos`。
- 排序：按视频 `created_at desc`。

### `latest_time` 游标分页到底是什么

你不懂这句很正常，因为它不是 CRUD 入门知识，而是 Feed 系统的核心习惯。

#### 先看 offset 分页

传统分页常写成：

```sql
select * from videos
order by created_at desc
limit 20 offset 0;

select * from videos
order by created_at desc
limit 20 offset 20;
```

问题是：Feed 流的数据会不断新增。假设你刚看完第一页，这时别人发布了 3 个新视频。你再请求 `offset 20` 时，原来第一页里的后 3 条可能被挤到第二页，你就会重复看到它们；也可能漏掉某些视频。

所以 offset 的问题是：

```text
数据静止时能用
数据持续新增时容易重复、漏数据
offset 越大，数据库扫描成本越高
```

#### 游标分页怎么做

游标分页不是说“我要第几页”，而是说：

```text
给我 created_at 早于某个时间点的视频
```

第一页请求：

```json
{
  "limit": 20,
  "latest_time": 0
}
```

SQL：

```sql
select * from videos
where status = 1
order by created_at desc
limit 20;
```

服务端拿到 20 条后，把最后一条视频的 `created_at` 转成毫秒时间戳，作为 `next_time` 返回。

第二页请求：

```json
{
  "limit": 20,
  "latest_time": 1718690000000
}
```

SQL：

```sql
select * from videos
where status = 1
  and created_at < from_unixtime(1718690000000 / 1000)
order by created_at desc
limit 20;
```

这样就算期间有新视频发布，它们的 `created_at` 比你的 `latest_time` 新，不会混进第二页，滚动体验更稳定。

#### 为什么后面还要 `created_at + id` 复合游标

只用 `latest_time` 有一个边界问题：多个视频可能有完全相同的 `created_at`。如果第二页使用 `created_at < latest_time`，同一时间创建但还没展示完的视频可能被跳过。

更稳的写法是复合游标：

```text
latest_time + latest_id
```

排序：

```sql
order by created_at desc, id desc
```

下一页条件：

```sql
where created_at < ?
   or (created_at = ? and id < ?)
```

V0.2 为了降低难度，可以先只做 `latest_time`。但你要知道它的升级方向就是 `created_at + id`。

### 开发顺序建议

1. 新建 `internal/video/model.go`，定义 `Video`、`Tag`、`VideoTag`。
2. 在 `main.go` 的 `AutoMigrate` 加上这三个模型。
3. 新建 `internal/video/repository.go`，先写 `CreateVideo`、`FindVideoByID`、`ListByAuthorID`、`ListLatest`。
4. 新建 `internal/video/service.go`，先写 `Publish` 和 `GetDetail`。
5. 新建 `internal/video/handler.go`，注册 `POST /video/publish`、`POST /video/getDetail`、`POST /video/listByAuthorID`。
6. 给 `/video/publish` 加 JWT 鉴权，从 context 取 `userID` 作为 `author_id`。
7. 在 router 中注册静态文件服务：`router.Static("/static/uploads", ".run/uploads")`。
8. 实现 `POST /video/uploadVideo`，保存 `.mp4`，返回 `play_url`。
9. 实现 `POST /video/uploadCover`，保存图片，返回 `cover_url`。
10. 新建 `internal/feed`，实现 `POST /feed/listLatest`。
11. 给 `ListLatest` 加 `latest_time` 游标分页。
12. 后半段再做 `#话题` 提取、`tags/video_tags` 入库和 `POST /feed/listByTag`。

### 难点预警详解

#### 1. 上传文件名不能直接用用户原始文件名

用户上传的文件名可能是：

```text
../../../xxx.mp4
同名文件.mp4
带空格和奇怪字符的文件名.mp4
```

如果你直接使用原始文件名，可能出现路径穿越、文件覆盖、URL 编码混乱。正确做法是：

```text
后端生成唯一文件名
保留原始扩展名
按用户 ID 和日期分目录
```

示例：

```text
.run/uploads/videos/1/20260618/1718690000123_abc123.mp4
```

#### 2. `play_url` 和本地路径必须分清

本地路径是服务器磁盘路径：

```text
.run/uploads/videos/1/20260618/xxx.mp4
```

浏览器访问 URL 是：

```text
/static/uploads/videos/1/20260618/xxx.mp4
```

数据库应该存 URL，不要存服务器磁盘路径。否则前端拿到后没法播放。

#### 3. 上传接口和发布接口要分开

不要在上传文件时就创建视频记录。推荐两步：

```text
uploadVideo -> 返回 play_url
uploadCover -> 返回 cover_url
publish -> 写 videos 表
```

原因：用户可能上传完视频后取消发布；也可能先上传视频再编辑标题封面。如果上传接口直接写 `videos`，会产生很多“未发布垃圾记录”。

#### 4. Feed 分页不要一开始用 offset

Feed 是时间流，数据一直插入。offset 在数据变化时容易重复或漏。`latest_time` 游标能表达“继续加载比上次最后一条更旧的数据”，更贴合 Feed 场景。

V0.2 先用：

```text
latest_time
```

V0.3 或 V1.0 升级：

```text
latest_time + latest_id
```

#### 5. `latest_time` 的单位要统一

前后端最容易在这里拧巴：秒、毫秒、数据库时间、时区。如果你返回毫秒时间戳，就所有接口都用毫秒；如果用秒，就都用秒。建议用毫秒，因为前端 JS 的 `Date.now()` 也是毫秒。

#### 6. 本地静态文件服务只适合开发环境

`router.Static("/static/uploads", ".run/uploads")` 适合 V0.2。生产环境通常会用 Nginx、OSS/S3/MinIO、CDN 来托管文件。你现在别急着上这些，但要知道本地静态服务不是最终形态。

#### 7. 话题提取不要写太复杂

V0.2 只需要支持简单格式：

```text
#Go #后端 #视频系统
```

先不要支持带空格的话题、特殊符号、emoji、复杂中文分词。用一个简单正则或字符扫描即可，重点是把 `tags` 和 `video_tags` 的关系跑通。

#### 8. 发布视频和写话题关系最好用事务

发布时可能同时写三类数据：

```text
videos
tags
video_tags
```

如果 `videos` 写成功，但 `video_tags` 写失败，你的数据就不完整。V0.2 后半段实现话题时，建议用 GORM transaction。先发布视频本体时可以不用事务。

---

## V0.3：互动社交闭环，全部同步写 MySQL

### 阶段目标

V0.2 已经完成“视频能发、Feed 能刷”。V0.3 要补齐社区互动闭环：

```text
用户看到视频
  -> 可以点赞 / 取消点赞
  -> 可以评论 / 删除自己的评论
  -> 可以关注作者 / 取关作者
  -> 可以查看用户主页统计
  -> 可以查看自己的点赞列表、关注列表、粉丝列表
  -> 可以发送和查看基础私信
```

这一版仍然全部同步写 MySQL，不上 Redis、RabbitMQ、Worker。原因很简单：你现在最该练的是事务、一致性、幂等和权限边界。同步版本写不稳，后面异步化只会把错误藏得更深。

V0.3 完成后，项目才真正从“视频 CRUD”进入“短视频社区系统”。

### 技术栈引入

- MySQL 事务：保证互动记录和冗余计数字段一致。
- 唯一索引：保证重复点赞、重复关注不会写出脏数据。
- GORM transaction：把“写记录 + 更新计数”放在同一个事务里。
- 行级条件更新：取消点赞、删除评论时，计数不能减成负数。
- 软删除：评论、点赞、关注可以先物理删除，但评论建议从一开始就软删除，后面通知和审计会更顺。

暂时不要引入 Redis。点赞状态、关注状态、用户主页统计确实会变成高频读，但你现在的数据量还没逼出缓存问题。先用 MySQL 把正确性做出来。

### 模块建议

建议新增三个业务模块，并少量扩展 `account`：

```text
internal/like/
  model.go        // Like 模型
  repository.go   // 点赞记录、点赞列表、点赞计数更新
  service.go      // 点赞幂等、取消点赞幂等、是否点赞
  handler.go      // /like 路由处理

internal/comment/
  model.go        // Comment 模型
  repository.go   // 评论写入、查询、软删除、评论计数更新
  service.go      // 评论发布、删除权限校验
  handler.go      // /comment 路由处理

internal/social/
  model.go        // Follow 模型，或命名 Social
  repository.go   // 关注关系、关注列表、粉丝列表
  service.go      // 关注、取关、禁止关注自己、幂等
  handler.go      // /social 路由处理

internal/message/
  model.go        // Message 模型
  repository.go   // 私信写入和会话查询
  service.go      // 发送、查询双方会话
  handler.go      // /message 路由处理
```

`account` 模块后面补一个用户主页接口：

```text
POST /account/getProfile
```

这个接口可以聚合 account、video、like、social 的统计数据。V0.3 可以先同步查 MySQL，V1.0 再考虑缓存。

---

### 建表思路

#### `likes`：视频点赞表

记录“哪个用户点赞了哪个视频”。一名用户对同一个视频最多只能点赞一次。

```sql
likes
- id bigint primary key
- account_id bigint not null
- video_id bigint not null
- created_at datetime
- updated_at datetime
- deleted_at datetime null
- unique(account_id, video_id)
```

字段含义：

| 字段 | 含义 | V0.3 使用方式 |
| --- | --- | --- |
| `id` | 点赞记录 ID | 主键 |
| `account_id` | 点赞用户 ID | 从 JWT 中取，不允许前端传当前用户 ID |
| `video_id` | 被点赞的视频 ID | 请求参数传入 |
| `created_at` | 点赞时间 | 我的点赞列表按它倒序 |
| `updated_at` | 更新时间 | GORM 自动维护 |
| `deleted_at` | 软删除时间 | 可选；V0.3 可以物理删除，想练软删除也可以保留 |

建议索引：

```sql
unique index uk_likes_account_video(account_id, video_id)
index idx_likes_video_id(video_id)
index idx_likes_account_created_at(account_id, created_at)
```

`videos.likes_count` 是冗余计数字段。点赞成功时 `+1`，取消点赞成功时 `-1`。

关键原则：

```text
只有点赞记录从“不存在”变成“存在”时，likes_count 才 +1
只有点赞记录从“存在”变成“不存在”时，likes_count 才 -1
重复点赞不能重复 +1
重复取消点赞不能重复 -1
```

#### `comments`：视频评论表

记录视频下的评论。V0.3 先做一级评论，不做楼中楼。

```sql
comments
- id bigint primary key
- video_id bigint not null
- account_id bigint not null
- content varchar(500) not null
- created_at datetime
- updated_at datetime
- deleted_at datetime null
```

字段含义：

| 字段 | 含义 | V0.3 使用方式 |
| --- | --- | --- |
| `id` | 评论 ID | 删除评论、通知扩展会用 |
| `video_id` | 评论所属视频 ID | 查询某视频评论列表 |
| `account_id` | 评论作者 ID | 从 JWT 中取 |
| `content` | 评论内容 | 必填，建议限制 1-500 字 |
| `created_at` | 评论时间 | 评论列表排序依据 |
| `updated_at` | 更新时间 | GORM 自动维护 |
| `deleted_at` | 删除时间 | 删除评论建议软删除 |

建议索引：

```sql
index idx_comments_video_created_at(video_id, created_at)
index idx_comments_account_id(account_id)
```

`videos.comments_count` 是冗余计数字段。评论发布成功 `+1`，评论删除成功 `-1`。

删除评论建议只允许：

```text
评论作者本人删除
```

V0.3 暂时不做视频作者删除他人评论，避免权限复杂化。

#### `follows`：关注关系表

记录“谁关注了谁”。如果你沿用原文档里的 `socials` 名字也可以，但我建议用 `follows`，语义更明确。

```sql
follows
- id bigint primary key
- follower_id bigint not null
- vlogger_id bigint not null
- created_at datetime
- updated_at datetime
- deleted_at datetime null
- unique(follower_id, vlogger_id)
```

字段含义：

| 字段 | 含义 | V0.3 使用方式 |
| --- | --- | --- |
| `id` | 关注记录 ID | 主键 |
| `follower_id` | 关注者 ID | 当前登录用户 |
| `vlogger_id` | 被关注者 ID | 请求参数传入 |
| `created_at` | 关注时间 | 关注列表排序 |
| `updated_at` | 更新时间 | GORM 自动维护 |
| `deleted_at` | 取关时间 | 可选；V0.3 可以物理删除 |

建议索引：

```sql
unique index uk_follows_follower_vlogger(follower_id, vlogger_id)
index idx_follows_follower_id(follower_id)
index idx_follows_vlogger_id(vlogger_id)
```

注意命名：

```text
follower_id = 谁发起关注
vlogger_id  = 被谁关注
```

不要写成 `user_id` 和 `to_user_id` 这种含糊名字，否则你后面写粉丝列表时一定会绕晕。

#### `messages`：基础私信表

V0.3 私信只做最简单的一对一文本消息，不做已读状态、不做会话表、不做 WebSocket。

```sql
messages
- id bigint primary key
- from_id bigint not null
- to_id bigint not null
- content varchar(1000) not null
- created_at datetime
- updated_at datetime
- deleted_at datetime null
```

字段含义：

| 字段 | 含义 | V0.3 使用方式 |
| --- | --- | --- |
| `id` | 消息 ID | 主键 |
| `from_id` | 发送者 ID | 当前登录用户 |
| `to_id` | 接收者 ID | 请求参数传入 |
| `content` | 消息内容 | 必填，建议限制 1-1000 字 |
| `created_at` | 发送时间 | 会话排序依据 |
| `updated_at` | 更新时间 | GORM 自动维护 |
| `deleted_at` | 删除时间 | V0.3 可以不用 |

建议索引：

```sql
index idx_messages_from_to_created_at(from_id, to_id, created_at)
index idx_messages_to_from_created_at(to_id, from_id, created_at)
```

查询双方会话时条件是：

```sql
where (from_id = A and to_id = B)
   or (from_id = B and to_id = A)
order by created_at desc
limit 50
```

---

### 路由设计

#### Like 接口

##### `POST /like/isLiked`

功能：判断当前登录用户是否点赞了某个视频。

- 鉴权：需要登录。
- 请求体：

```json
{
  "video_id": 1
}
```

- 返回：

```json
{
  "is_liked": true
}
```

用途：视频详情页、Feed 卡片渲染时显示点赞状态。

##### `POST /like/like`

功能：点赞视频。

- 鉴权：需要登录。
- 请求体：

```json
{
  "video_id": 1
}
```

- 返回：

```json
{
  "status": "ok",
  "likes_count": 12
}
```

服务端行为：

```text
1. 校验 video_id 非空。
2. 查询视频是否存在且 status = 1。
3. 在事务里创建 likes 记录。
4. 如果创建成功，videos.likes_count + 1。
5. 如果唯一索引冲突，说明已经点过赞，直接当作成功返回，不重复 +1。
```

##### `POST /like/unlike`

功能：取消点赞。

- 鉴权：需要登录。
- 请求体：

```json
{
  "video_id": 1
}
```

- 返回：

```json
{
  "status": "ok",
  "likes_count": 11
}
```

服务端行为：

```text
1. 查询点赞记录是否存在。
2. 如果不存在，直接当作成功返回，不重复 -1。
3. 如果存在，在事务里删除 likes 记录。
4. 删除成功后 videos.likes_count - 1。
5. 更新计数时加条件 likes_count > 0，避免负数。
```

##### `POST /like/listMyLikedVideos`

功能：查询当前用户点赞过的视频列表。

- 鉴权：需要登录。
- 请求体：

```json
{
  "limit": 20,
  "latest_time": 0
}
```

- 返回：

```json
{
  "videos": [],
  "next_time": 1718690000000,
  "has_more": true
}
```

排序建议：按点赞时间倒序，也就是 `likes.created_at desc`，不是视频发布时间。

#### Comment 接口

##### `POST /comment/publish`

功能：发布评论。

- 鉴权：需要登录。
- 请求体：

```json
{
  "video_id": 1,
  "content": "写得不错"
}
```

- 返回：

```json
{
  "comment": {
    "id": 1,
    "video_id": 1,
    "account_id": 2,
    "content": "写得不错",
    "created_at": "2026-06-22T12:00:00+08:00"
  },
  "comments_count": 5
}
```

服务端行为：

```text
1. 校验 video_id 和 content。
2. content TrimSpace 后不能为空。
3. 限制 content 长度，比如 500 字。
4. 查询视频是否存在且 status = 1。
5. 事务内创建 comment，并 videos.comments_count + 1。
```

##### `POST /comment/listAll`

功能：查询某个视频的评论列表。

- 鉴权：V0.3 可以不登录。
- 请求体：

```json
{
  "video_id": 1,
  "limit": 20,
  "latest_time": 0
}
```

- 返回：

```json
{
  "comments": [],
  "next_time": 1718690000000,
  "has_more": true
}
```

排序建议：先按 `created_at desc`。后续可以改成热评排序。

##### `POST /comment/delete`

功能：删除评论。

- 鉴权：需要登录。
- 请求体：

```json
{
  "comment_id": 1
}
```

- 返回：

```json
{
  "status": "ok",
  "comments_count": 4
}
```

服务端行为：

```text
1. 查询评论是否存在。
2. 校验 comment.account_id == 当前登录用户 ID。
3. 事务内软删除评论，并 videos.comments_count - 1。
4. 如果评论已经删除，返回成功即可，不重复 -1。
```

#### Social 接口

##### `POST /social/isFollowing`

功能：判断当前登录用户是否关注了某个用户。

- 鉴权：需要登录。
- 请求体：

```json
{
  "vlogger_id": 2
}
```

- 返回：

```json
{
  "is_following": true
}
```

##### `POST /social/follow`

功能：关注用户。

- 鉴权：需要登录。
- 请求体：

```json
{
  "vlogger_id": 2
}
```

- 返回：

```json
{
  "status": "ok"
}
```

服务端行为：

```text
1. 当前登录用户作为 follower_id。
2. 请求里的 vlogger_id 是被关注者。
3. 禁止 follower_id == vlogger_id。
4. 校验被关注用户存在。
5. 创建 follows 记录。
6. 如果唯一索引冲突，说明已经关注，直接当作成功。
```

##### `POST /social/unfollow`

功能：取关用户。

- 鉴权：需要登录。
- 请求体：

```json
{
  "vlogger_id": 2
}
```

- 返回：

```json
{
  "status": "ok"
}
```

服务端行为：

```text
1. 查询关注关系。
2. 如果不存在，直接当作成功。
3. 如果存在，删除关注关系。
```

##### `POST /social/listFollowers`

功能：查询某个用户的粉丝列表。

- 鉴权：V0.3 可以不登录。
- 请求体：

```json
{
  "account_id": 2,
  "limit": 20,
  "latest_time": 0
}
```

- 返回：

```json
{
  "accounts": [],
  "next_time": 1718690000000,
  "has_more": true
}
```

粉丝列表查询：

```text
where vlogger_id = account_id
join accounts on accounts.id = follows.follower_id
order by follows.created_at desc
```

##### `POST /social/listFollowings`

功能：查询某个用户关注的人。

- 鉴权：V0.3 可以不登录。
- 请求体：

```json
{
  "account_id": 1,
  "limit": 20,
  "latest_time": 0
}
```

- 返回：

```json
{
  "accounts": [],
  "next_time": 1718690000000,
  "has_more": true
}
```

关注列表查询：

```text
where follower_id = account_id
join accounts on accounts.id = follows.vlogger_id
order by follows.created_at desc
```

#### Account 扩展接口

##### `POST /account/getProfile`

功能：用户主页信息聚合。

- 鉴权：V0.3 可以不登录。
- 请求体：

```json
{
  "account_id": 1
}
```

- 返回：

```json
{
  "account": {
    "id": 1,
    "username": "user",
    "avatar_url": "",
    "bio": ""
  },
  "stats": {
    "videos_count": 10,
    "likes_count": 233,
    "followers_count": 20,
    "followings_count": 8
  }
}
```

统计方式 V0.3 可以先实时查询：

```text
videos_count     = count(videos where author_id = account_id and status = 1)
likes_count      = sum(videos.likes_count where author_id = account_id and status = 1)
followers_count  = count(follows where vlogger_id = account_id)
followings_count = count(follows where follower_id = account_id)
```

V1.0 再考虑把这些统计缓存到 Redis。

#### Message 接口

##### `POST /message/send`

功能：发送私信。

- 鉴权：需要登录。
- 请求体：

```json
{
  "to_id": 2,
  "content": "你好"
}
```

- 返回：

```json
{
  "message": {
    "id": 1,
    "from_id": 1,
    "to_id": 2,
    "content": "你好",
    "created_at": "2026-06-22T12:00:00+08:00"
  }
}
```

服务端行为：

```text
1. from_id 从 JWT 中取。
2. 校验 to_id 用户存在。
3. 禁止给自己发私信，可以先禁，也可以允许；建议先禁止。
4. content TrimSpace 后不能为空，长度限制 1000。
5. 写入 messages。
```

##### `POST /message/list`

功能：查询和某个用户的最近私信。

- 鉴权：需要登录。
- 请求体：

```json
{
  "with_account_id": 2,
  "limit": 50,
  "latest_time": 0
}
```

- 返回：

```json
{
  "messages": [],
  "next_time": 1718690000000,
  "has_more": true
}
```

查询条件：

```sql
where (
  from_id = current_user_id and to_id = with_account_id
) or (
  from_id = with_account_id and to_id = current_user_id
)
```

---

### 开发顺序建议

不要四个模块同时开写。按下面顺序推进，每一步都能独立测试。

#### 第一阶段：点赞闭环

1. 新建 `internal/like`，定义 `Like` 模型。
2. 在 `cmd/main.go` 的 `AutoMigrate` 加入 `like.Like`。
3. 写 `LikeRepo`：`Exists`、`Create`、`Delete`、`ListMyLikedVideos`。
4. 在 `VideoRepo` 或新增共享方法中写：`IncreaseLikesCount`、`DecreaseLikesCount`。
5. 写 `LikeService.IsLiked`。
6. 写 `LikeService.Like`：事务内 `CreateLike + IncreaseLikesCount`。
7. 写 `LikeService.Unlike`：事务内 `DeleteLike + DecreaseLikesCount`。
8. 写 `LikeHandler` 并注册 `/like` 路由组。
9. 用同一个用户重复点赞、重复取消点赞，确认计数不会错。

#### 第二阶段：评论闭环

1. 新建 `internal/comment`，定义 `Comment` 模型。
2. `AutoMigrate` 加入 `comment.Comment`。
3. 写 `CommentRepo`：`Create`、`FindByID`、`SoftDelete`、`ListByVideoID`。
4. 写视频评论计数更新方法：`IncreaseCommentsCount`、`DecreaseCommentsCount`。
5. 写 `CommentService.Publish`：事务内 `CreateComment + IncreaseCommentsCount`。
6. 写 `CommentService.Delete`：校验作者后，事务内 `SoftDelete + DecreaseCommentsCount`。
7. 写 `CommentService.ListAll`。
8. 注册 `/comment` 路由组。
9. 测试删除别人评论、重复删除、空评论、超长评论。

#### 第三阶段：关注闭环

1. 新建 `internal/social`，定义 `Follow` 模型。
2. `AutoMigrate` 加入 `social.Follow`。
3. 写 `FollowRepo`：`Exists`、`Create`、`Delete`、`ListFollowers`、`ListFollowings`。
4. 写 `SocialService.Follow`：禁止关注自己，校验被关注者存在，唯一索引冲突视为成功。
5. 写 `SocialService.Unfollow`：不存在也返回成功。
6. 写 `SocialService.IsFollowing`。
7. 写粉丝列表和关注列表。
8. 注册 `/social` 路由组。
9. 测试关注自己、重复关注、重复取关。

#### 第四阶段：用户主页聚合

1. 在 `account` 模块新增 `GetProfile`。
2. 需要查询 account 基本信息、作品数、获赞数、粉丝数、关注数。
3. 可以让 `AccountService` 依赖多个 repository，但不要让 handler 直接查多个 repo。
4. 注册 `POST /account/getProfile`。
5. 测试无作品、无粉丝、新用户的返回。

#### 第五阶段：基础私信

1. 新建 `internal/message`，定义 `Message` 模型。
2. `AutoMigrate` 加入 `message.Message`。
3. 写 `MessageRepo.Create` 和 `ListConversation`。
4. 写 `MessageService.Send`：校验接收者存在、内容非空。
5. 写 `MessageService.List`：只允许查自己参与的会话。
6. 注册 `/message` 路由组。
7. 测试双方互发、分页、空内容、给不存在用户发消息。

---

### 难点预警详解

#### 1. 点赞和关注必须幂等

幂等的意思是：同一个操作执行多次，结果应该和执行一次一样。

点赞例子：

```text
第一次点赞：创建 likes 记录，likes_count + 1
第二次点赞：发现已经点赞，不报错，也不 +1
```

取消点赞例子：

```text
第一次取消：删除 likes 记录，likes_count - 1
第二次取消：发现已经没有点赞，不报错，也不 -1
```

关注同理。你必须靠唯一索引兜底，不能只靠“先查再插”。并发下两个请求可能同时查到不存在，然后同时插入。

#### 2. 计数字段必须和记录表放在同一个事务

错误写法：

```text
先插入 likes
再更新 videos.likes_count
```

如果第二步失败，就会出现：点赞记录存在，但视频点赞数没加。

正确写法：

```text
transaction:
  insert likes
  update videos set likes_count = likes_count + 1
```

取消点赞、评论发布、评论删除都一样。

#### 3. 计数不能减成负数

取消点赞和删除评论都要注意：

```sql
update videos
set likes_count = likes_count - 1
where id = ? and likes_count > 0
```

评论数同理。即使业务正常不会减到负数，数据库更新也应该兜底。

#### 4. 先查再插不是并发安全的

这段逻辑不可靠：

```text
if not exists like:
  create like
  likes_count + 1
```

两个并发请求可能都看到不存在。正确做法是：

```text
create like
如果成功：likes_count + 1
如果唯一索引冲突：说明已经点赞，返回成功，不更新计数
```

#### 5. 删除评论要做权限校验

不能让用户传一个 `comment_id` 就删除。必须查评论：

```text
comment.account_id == current_user_id
```

否则任何登录用户都能删别人的评论。

#### 6. 关注方向最容易写反

记住：

```text
follower_id 关注了 vlogger_id
```

粉丝列表：

```text
谁关注了我 -> where vlogger_id = 我的ID
```

关注列表：

```text
我关注了谁 -> where follower_id = 我的ID
```

#### 7. 用户主页不要在 handler 里拼数据

`getProfile` 会查多个表。不要因为它是聚合接口，就在 handler 里到处调用 repo。

推荐结构：

```text
handler 只解析 account_id
service 负责组织聚合逻辑
repository 负责各自的数据查询
```

#### 8. 私信不是实时聊天

V0.3 的私信只是“留言式私信”：

```text
send 写入 MySQL
list 拉取最近消息
```

不要现在就上 WebSocket、SSE、已读回执、会话表。那些放到 V1.5 通知系统再处理。


---

## V1.0：Redis 高并发读优化

### 阶段目标

V0.3 已经把点赞、评论、关注、私信、用户主页这条社区闭环跑通了。现在开始引入 Redis 是合理的，因为系统已经出现了明确的高频读场景：

- Feed 最新流读取频繁。
- 视频详情读取频繁。
- 用户主页聚合统计读取频繁。
- 点赞状态、关注状态读取频繁。
- 热榜需要按热度快速排序。
- 登录、上传、发布、评论、私信等接口需要基础限流。

这一阶段的目标不是“把 Redis 塞进所有地方”，而是用 Redis 解决真实的读性能问题，并让你能讲清楚缓存一致性、降级和 key 设计。

V1.0 暂时不要做：

- 不要一上来引入 MQ。
- 不要一上来做异步点赞。
- 不要一上来做复杂推荐算法。
- 不要把所有接口都缓存一遍。
- 不要为了简历堆中间件，必须能解释每个 Redis key 的价值和一致性边界。

### 技术栈引入

- `go-redis/v9`：Redis 客户端。
- Redis `STRING`：token、视频详情、用户主页聚合结果。
- Redis `ZSET`：Feed 最新时间线、热榜。
- Redis `INCR + EXPIRE`：固定窗口限流。
- Redis `SETNX`：缓存击穿互斥锁，可后置实现。

必须坚持几个原则：

- MySQL 仍是最终数据源。
- Redis 首先作为缓存和加速层，不要一开始就当主存储。
- Redis 不可用时，核心业务应能降级回 MySQL，只是变慢，不能直接全站挂掉。
- 每个 Redis key 都要能讲清楚解决什么问题。
- 缓存必须考虑失效策略，不能只写读缓存不写删除/更新逻辑。

### Redis Key 设计

建议先统一 key 前缀，不要在业务代码里散落裸字符串。

```text
myfeed:account:token:{account_id}       -> 当前登录 token，TTL 不超过 JWT 过期时间
myfeed:video:detail:{video_id}          -> 视频详情 JSON，短 TTL
myfeed:profile:{account_id}             -> 用户主页聚合结果 JSON，短 TTL
myfeed:feed:latest                      -> 最新 Feed ZSET
myfeed:rank:hot                         -> 热榜 ZSET
myfeed:rate:{scene}:{identifier}        -> 限流计数
```

说明：

- `account:token` 是为了减少鉴权时的 MySQL 查询，但不能破坏登出语义。
- `video:detail` 适合第一批做缓存，因为边界清楚、访问频繁。
- `profile` 是聚合接口，缓存收益明显，但要接受短暂不一致或做好主动失效。
- `feed:latest` 适合用 ZSET 保存最新视频 ID。
- `rank:hot` 适合用 ZSET 保存热度分数。
- `rate` 用于登录、上传、发布、评论、私信、点赞等场景限流。

### MySQL 和索引补充

`videos.popularity` 在 V0.2 已预留，V1.0 热榜可以开始使用。

V1.0 不需要重新设计大表，重点是补齐支撑游标和高频查询的复合索引：

```text
videos:   (status, created_at, id)
videos:   (author_id, status, created_at, id)
comments: (video_id, created_at, id)
likes:    (account_id, created_at, id)
follows:  (vlogger_id, created_at, id)
follows:  (follower_id, created_at, id)
messages: (from_id, to_id, created_at, id)
messages: (to_id, from_id, created_at, id)
```

如果后面私信继续增强，可以考虑新增会话表或 `conversation_id`，否则双方消息的 OR 查询会越来越难优化。

### 开发顺序建议

#### 1. Redis 基础设施

这一小步不要急着改 JWT、视频详情或 Feed 业务。先把 Redis 作为一个稳定的基础设施接进项目，否则后面每个业务模块都会自己处理连接、key、降级和错误，代码很快失控。

当前项目已经有：

```text
internal/config/config.go            -> 已有 RedisConfig 和 REDIS_* 环境变量覆盖
configs/config.yaml                  -> 已有 redis 配置块
internal/middleware/redis/redis.go   -> 已有 go-redis client 雏形
```

但 Redis 不是 Gin middleware，它是缓存基础设施。建议后续把它收敛到更准确的位置：

```text
internal/cache/
  redis.go
  keys.go
```

如果你暂时不移动文件，也要按 `internal/cache` 的职责来设计：只负责 Redis client 生命周期、key 生成、基础封装，不要在里面写 account、video、feed 的业务逻辑。

##### 1.1 配置契约

先把 Redis 配置定成稳定契约，后续业务缓存都基于这套配置运行。

建议配置项：

```yaml
redis:
  host: localhost
  port: 6379
  password:
  db: 0
  key_prefix: myfeed
  enabled: true
```

对应环境变量：

```text
REDIS_HOST
REDIS_PORT
REDIS_PASSWORD
REDIS_DB
REDIS_KEY_PREFIX
REDIS_ENABLED
```

说明：

- `key_prefix` 必须可配置，避免本地、测试、生产环境共用 Redis 时 key 冲突。
- `enabled=false` 时不初始化 Redis，所有缓存逻辑直接走 MySQL。
- 配置文件提供本地默认值，环境变量优先级更高，保持当前项目已有的 `config.yaml + .env` 叠加方式。
- 不要把 token TTL、视频详情 TTL 这类业务 TTL 全塞进基础配置第一版。可以先在各业务缓存模块里定义常量，等 TTL 多了再抽到配置。

##### 1.2 Client 封装目标

`internal/cache` 里不要把 `*redis.Client` 直接暴露到所有业务层。第一版可以提供一个很薄的包装：

```go
type Client struct {
    rdb       *redis.Client
    keyPrefix string
    enabled   bool
}

func New(cfg config.RedisConfig) (*Client, error)
func (c *Client) Ping(ctx context.Context) error
func (c *Client) Close() error
func (c *Client) Enabled() bool
func (c *Client) Key(parts ...string) string
func (c *Client) Redis() *redis.Client
```

设计要求：

- `New` 只创建 client，不做长时间阻塞。
- `Ping` 单独执行，并由 `cmd/main.go` 控制超时。
- `Close` 在服务退出时调用，避免连接泄漏。
- `Enabled()` 用于业务层快速判断是否启用缓存。
- `Key(parts ...string)` 统一拼接 `myfeed:video:detail:1` 这种 key，业务层不要手写完整 key。
- 如果暴露 `Redis()`，也只作为过渡方案。后续更好的做法是给 token、video、profile 分别定义小接口，业务层不直接依赖 go-redis。

##### 1.3 启动生命周期

`cmd/main.go` 的 Redis 初始化顺序建议放在 DB 初始化之后、路由注册之前：

```text
加载 .env
加载 config.yaml
初始化 MySQL
AutoMigrate
初始化 Redis client
Ping Redis，设置短超时
注册路由，把 cache 依赖注入需要的 service
启动 HTTP server
```

学习项目建议采用“允许降级启动”的策略：

```text
Redis 配置关闭 -> cache disabled，正常启动
Redis client 创建失败 -> 打日志，cache disabled，正常启动
Redis Ping 失败 -> 打日志，cache disabled，正常启动
Redis 运行中失败 -> 当前请求回源 MySQL，记录日志或指标
```

不要在 V1.0 基础设施阶段因为 Redis 挂了就 `log.Fatal`。这个项目的主数据源仍是 MySQL，Redis 是加速层。Redis 不可用时接口可以变慢，但账号、视频、点赞、评论、关注、私信这些核心链路不能直接不可用。

启动日志至少要能看出：

```text
redis disabled by config
redis connected addr=localhost:6379 db=0 prefix=myfeed
redis unavailable, cache disabled: dial tcp ...
```

##### 1.4 超时与错误边界

Redis 操作必须带 `context.Context`，不要在业务代码里使用没有超时的 `context.Background()`。

建议：

- 启动 `Ping` 超时：`300ms` 到 `1s`。
- 单次缓存读写超时：`50ms` 到 `200ms`。
- `redis.Nil` 只表示 cache miss，不是业务错误。
- 连接失败、超时、序列化失败属于缓存错误，第一版都应该回源 MySQL。
- MySQL 回源失败才是业务失败，不能把 Redis miss 当成接口失败。

示例语义：

```text
GET video detail cache
redis.Nil              -> miss，查 MySQL
context deadline       -> 记录日志，查 MySQL
redis connection error -> 记录日志，查 MySQL
json unmarshal error   -> 删除坏缓存，查 MySQL
MySQL error            -> 返回业务错误
```

##### 1.5 Key 生成规范

基础设施阶段先把 key 生成函数定好。不要让各业务模块自己拼字符串。

建议：

```go
func (c *Client) AccountTokenKey(accountID uint) string
func (c *Client) VideoDetailKey(videoID uint) string
func (c *Client) ProfileKey(accountID uint) string
func (c *Client) FeedLatestKey() string
func (c *Client) RankHotKey() string
func (c *Client) RateKey(scene string, identifier string) string
```

生成结果保持和前文 key 设计一致：

```text
myfeed:account:token:{account_id}
myfeed:video:detail:{video_id}
myfeed:profile:{account_id}
myfeed:feed:latest
myfeed:rank:hot
myfeed:rate:{scene}:{identifier}
```

注意：

- key 里的 ID 使用十进制字符串，不要混用用户名、昵称等可变字段。
- `scene` 这类外部输入必须限制字符集，避免生成不可控 key。
- 测试环境可以使用 `myfeed:test` 前缀，避免污染本地开发数据。

##### 1.6 包依赖方向

推荐依赖方向：

```text
cmd/main.go
  -> internal/config
  -> internal/cache
  -> internal/router
  -> feature handlers/services

feature service
  -> feature repository
  -> small cache interface
```

不要让 `internal/cache` 反向 import `account`、`video`、`feed`。基础设施包只知道 Redis，不知道业务模型。业务模块如果需要缓存，自己定义小接口，例如：

```go
type TokenCache interface {
    GetToken(ctx context.Context, accountID uint) (string, bool, error)
    SetToken(ctx context.Context, accountID uint, token string, ttl time.Duration) error
    DeleteToken(ctx context.Context, accountID uint) error
}
```

第一版可以先在具体业务模块里实现这些接口，底层使用 `internal/cache.Client`。这样以后要替换缓存实现或做 mock 测试，不会牵动所有 service。

##### 1.7 基础设施验收标准

这一小步完成后，先不要急着写 token 缓存。至少满足下面验收项：

1. `go test ./...` 能通过，Redis 不启动时也不影响现有测试。
2. `configs/config.yaml` 和 `.env` 都能覆盖 Redis 地址、密码、DB、key prefix、enabled。
3. Redis 正常启动时日志明确显示连接成功。
4. Redis 未启动时服务仍可启动，现有 MySQL 业务接口仍可用。
5. 所有 Redis key 都通过统一函数生成，没有业务层裸写完整 key。
6. client 有 `Close`，`main.go` 能在退出时释放连接。
7. 有最小单元测试覆盖 key 生成、`enabled=false`、`redis.Nil` 被识别为 miss 这几类基础语义。

#### 2. JWT token 缓存

这是 V1.0 最适合作为第一个 Redis 业务改造的功能。原因是当前项目已经有明确的服务端撤销语义：

```text
登录成功 -> 生成 JWT -> 写入 accounts.token -> 返回前端
鉴权 -> 解析 JWT -> 查 accounts.token -> 和请求 token 比对
登出 -> 清空 accounts.token
```

引入 Redis 后，不改变这个业务语义。MySQL 的 `accounts.token` 仍然是最终可信来源，Redis 只用于减少每次鉴权时的 MySQL 查询。

改造目标：

```text
鉴权 -> 先查 Redis token
Redis miss -> 查 MySQL accounts.token
MySQL 命中 -> 回填 Redis
登出 -> 删除 Redis token + 清空 MySQL token
```

##### 2.1 涉及模块和依赖注入

建议在基础设施之上增加一个专门的 token cache 封装。不要让 JWT 中间件直接到处写 `cache.Redis().Get(...)`。

建议结构：

```text
internal/cache/
  redis.go              -> Redis client、Ping、Close、Enabled、Key
  keys.go               -> AccountTokenKey 等 key 生成函数

internal/account/
  token_cache.go        -> TokenCache 接口和 RedisTokenCache 实现

internal/middleware/jwt/
  jwtAuth.go            -> 鉴权流程，依赖 accountRepo + tokenCache
```

接口建议：

```go
type TokenCache interface {
    GetToken(ctx context.Context, accountID uint) (token string, hit bool, err error)
    SetToken(ctx context.Context, accountID uint, token string, ttl time.Duration) error
    DeleteToken(ctx context.Context, accountID uint) error
}
```

三个方法对于这个业务的意义：

- `GetToken`：鉴权时读取 `myfeed:account:token:{account_id}`，命中后直接和请求 token 比对。
- `SetToken`：登录成功后写入 Redis，或鉴权 MySQL 回源成功后回填 Redis。
- `DeleteToken`：登出时删除 Redis token，避免旧 token 在缓存 TTL 内继续命中。

`TokenCache` 的实现内部调用基础 Redis client：

```text
GetToken
  -> cache.Client.Enabled()
  -> cache.Client.AccountTokenKey(accountID)
  -> redis.Client.Get(ctx, key)

SetToken
  -> cache.Client.Enabled()
  -> cache.Client.AccountTokenKey(accountID)
  -> redis.Client.Set(ctx, key, token, ttl)

DeleteToken
  -> cache.Client.Enabled()
  -> cache.Client.AccountTokenKey(accountID)
  -> redis.Client.Del(ctx, key)
```

`redis.Nil` 在 `GetToken` 里转换成 `(token="", hit=false, err=nil)`，不要把缓存未命中当成错误。

##### 2.2 从系统初始化开始的完整流程

系统启动时：

```text
cmd/main.go
  1. godotenv.Load()
  2. config.LoadLoaclDev(configPath)
  3. db.NewDB(cfg.Database)
  4. db.AutoMigrate(sqlDB)
  5. cache.New(cfg.Redis)
  6. cache.Ping(ctx)
  7. account.NewRedisTokenCache(cacheClient)
  8. router.SetRouter(sqlDB, tokenCache)
```

关键点：

- `cache.New(cfg.Redis)` 只根据配置创建 client，并保存 `enabled`、`keyPrefix` 等信息。
- `cache.Ping(ctx)` 验证 Redis 是否可用，失败时把 cache 标记为 disabled，服务继续启动。
- `NewRedisTokenCache(cacheClient)` 不连接 Redis，只保存依赖。
- `router.SetRouter` 把 `tokenCache` 注入 `accountService` 和 `JWTAuth` 中间件。

路由初始化后，所有受保护接口仍然走 `JWTAuth`。区别只是 `JWTAuth` 先尝试 Redis，再决定是否查 MySQL。

##### 2.3 登录流程：写 MySQL 后写 Redis

当前登录流程是 `AccountService.Login`：

```text
FindAccountByName(username)
bcrypt.CompareHashAndPassword(...)
auth.GenerateToken(acc.ID, acc.Username)
repo.UpdateToken(acc.ID, token)
return token
```

加入 Redis 后建议流程：

```text
1. 查账号、校验密码。
2. 生成 JWT。
3. 写 MySQL：accounts.token = token。
4. 写 Redis：myfeed:account:token:{account_id} = token。
5. 返回 token。
```

为什么先写 MySQL，再写 Redis：

- MySQL 是最终可信来源，必须先保证服务端撤销依据已经落库。
- Redis 写失败不应该导致登录失败，因为后续鉴权可以从 MySQL 回源。
- 如果 Redis 写失败，只记录日志，返回登录成功。下一次鉴权时 Redis miss，再查 MySQL 并回填。

调用关系示例：

```text
AccountService.Login
  -> repo.UpdateToken(accountID, token)
  -> tokenCache.SetToken(ctx, accountID, token, ttl)
       -> cache.AccountTokenKey(accountID)
       -> rdb.Set(ctx, key, token, ttl)
```

TTL 计算：

- JWT 当前过期时间是 2 小时。
- Redis token TTL 不得超过 JWT 过期时间。
- 第一版可以固定 `2 * time.Hour`，更严谨的做法是从 `claims.ExpiresAt` 计算剩余时间。

##### 2.4 鉴权流程：先 Redis，miss 再 MySQL

当前 `JWTAuth` 每次都会：

```text
读取 Authorization
解析 Bearer token
auth.ParseToken(tokenString)
accountRepo.FindAccountByID(claims.AccountID)
比较 acc.Token == tokenString
写入 gin.Context: userID, username
```

加入 Redis 后建议流程：

```text
1. 读取 Authorization。
2. 校验 Bearer 格式。
3. auth.ParseToken(tokenString)，先确认 JWT 签名和过期时间有效。
4. tokenCache.GetToken(ctx, claims.AccountID)。
5. Redis hit：
   - cachedToken == tokenString -> 鉴权通过。
   - cachedToken != tokenString -> 返回 401。
6. Redis miss 或 Redis 错误：
   - accountRepo.FindAccountByID(claims.AccountID)。
   - acc.Token == tokenString -> 鉴权通过，并 tokenCache.SetToken 回填 Redis。
   - acc.Token != tokenString -> 返回 401。
7. 设置 gin.Context 中的 userID、username。
```

调用关系示例：

```text
JWTAuth
  -> auth.ParseToken(tokenString)
  -> tokenCache.GetToken(ctx, claims.AccountID)
       -> cache.AccountTokenKey(accountID)
       -> rdb.Get(ctx, key)
  -> accountRepo.FindAccountByID(accountID)        // 仅 miss 或 Redis 异常时
  -> tokenCache.SetToken(ctx, accountID, token, ttl) // MySQL 校验成功后回填
```

这里有两个重要细节：

- 必须先 `ParseToken`，再查 Redis。否则攻击者随便传一个字符串，也会打到 Redis。
- Redis hit 且 token 不相等时，可以直接 401，不需要再查 MySQL。因为登录成功或登出都会维护这个 key；如果担心缓存污染，第一版可以在不相等时查一次 MySQL 兜底，但逻辑会更复杂。

##### 2.5 登出流程：删除 Redis，再清空 MySQL

当前登出流程是：

```text
JWTAuth 通过
AccountService.Logout(userID)
repo.UpdateToken(userID, "")
```

加入 Redis 后建议流程：

```text
1. JWTAuth 已经确认当前 token 有效。
2. tokenCache.DeleteToken(ctx, userID)。
3. repo.UpdateToken(userID, "")。
4. 返回登出成功。
```

为什么登出要优先处理 Redis：

- 如果先清空 MySQL，但 Redis 删除失败，旧 token 仍可能在 TTL 内通过 Redis hit。
- 先删 Redis 后，如果 MySQL 清空失败，请求应返回失败；因为最终可信来源仍然认为 token 有效。
- 如果 Redis 删除失败，第一版建议返回 500，不要假装登出成功。否则用户以为已退出，但旧 token 可能还能访问接口。

调用关系示例：

```text
AccountService.Logout
  -> tokenCache.DeleteToken(ctx, accountID)
       -> cache.AccountTokenKey(accountID)
       -> rdb.Del(ctx, key)
  -> repo.UpdateToken(accountID, "")
```

如果你选择“Redis 删除失败也继续清 MySQL”，那 `JWTAuth` 在 Redis hit 时就不能直接信任缓存，必须在关键场景回查 MySQL。学习阶段不建议这么绕，先采用“登出删缓存失败则登出失败”的策略更清晰。

##### 2.6 异常分支和一致性边界

必须明确这些情况怎么处理：

```text
登录：MySQL 写 token 成功，Redis Set 失败
  -> 登录成功，记录日志；后续鉴权 miss 回源 MySQL。

登录：MySQL 写 token 失败
  -> 登录失败，不写 Redis。

鉴权：Redis Get 超时或连接失败
  -> 查 MySQL；MySQL 校验成功后尝试回填 Redis。

鉴权：Redis hit，但 token 不一致
  -> 返回 401。

鉴权：Redis miss，MySQL token 为空
  -> 返回 401，说明已登出或 token 已失效。

登出：Redis Delete 失败
  -> 返回失败，不清空 MySQL，避免缓存中旧 token 继续有效但用户以为已退出。

登出：Redis Delete 成功，MySQL 清空失败
  -> 返回失败；此时下次鉴权 Redis miss 后仍可能从 MySQL 回源成功，所以需要日志暴露这个问题。
```

这个设计的核心取舍是：

- 登录和鉴权阶段，Redis 失败可以降级。
- 登出阶段，Redis 删除失败不能静默忽略。
- MySQL 始终是最终可信来源，但 Redis hit 会成为高频鉴权路径，所以所有会改变 token 有效性的写操作都必须维护 Redis。

注意：

- token 缓存 TTL 不应超过 JWT 自身过期时间。
- Redis token 只是加速，不能让它破坏登出语义。
- 登出时 Redis 和 MySQL 都要处理。
- 如果 Redis 删除失败，不能让数据库 token 仍然有效却前端以为登出成功。这里要明确错误处理策略。
- 给 `TokenCache` 写单元测试时，至少覆盖 hit、miss、Set 失败、Delete 失败、disabled 这几类情况。

#### 3. 视频详情缓存

使用 Cache Aside 模式：

```text
读 Redis -> miss -> 读 MySQL -> 写 Redis -> 返回
```

写操作失效策略：

- 发布视频后可以不立刻缓存详情。
- 修改视频、删除视频、改变状态时要删除详情缓存。
- 点赞数、评论数变化后，学习阶段建议先删除详情缓存，而不是原地修改缓存 JSON。

原因很简单：先删缓存更稳。你现在不要为了少一次 DB 查询，把一致性写复杂。

#### 4. 用户主页聚合缓存

`getProfile` 会聚合多张表：

```text
account 基本信息
videos_count
likes_count
followers_count
followings_count
```

V1.0 可以把主页结果缓存短 TTL，例如 30 秒到 120 秒。

需要知道哪些写操作会影响它：

- 关注/取关会影响粉丝数和关注数。
- 点赞/取消点赞会影响作者获赞数。
- 发布/删除视频会影响作品数。

第一版可以只做短 TTL，降低主动失效复杂度。等你把缓存读链路跑通，再考虑在相关写操作后主动删除 `myfeed:profile:{account_id}`。

#### 5. Feed 最新流缓存

`/feed/listLatest` 是首页高频读接口。当前 MySQL 版本已经使用 `created_at DESC, id DESC` 排序，并通过 `latest_time + latest_id` 做复合游标。Redis 版本不能改变这个分页语义，否则同一套前端参数在 MySQL 页和 Redis 页之间会出现重复或漏数据。

第一版建议把 Redis 只当作“最新视频 ID 热索引”，不要把完整视频列表大 JSON 直接缓存到一个 key 里。原因是视频详情里有点赞数、评论数、封面等字段，后面会被详情缓存复用；Feed 只负责快速拿到一批最新 `video_id`。

建议使用 Redis ZSET：

```text
key:   myfeed:feed:latest
score: created_at 毫秒时间戳
member: 固定宽度 video_id 字符串，例如 00000000000000012345
```

为什么 member 要固定宽度：

- Redis ZSET 先按 score 排序，同 score 时按 member 字典序排序。
- 当前业务排序要求同一 `created_at` 下 `id DESC`。
- 如果 member 直接用 `"9"`、`"10"`，字典序和数字顺序不一致；固定宽度后，反向字典序才和数字降序一致。

如果你不想依赖同 score 的 member 排序，第一版也可以规定 Redis 只服务第一页，深分页继续走 MySQL。但如果 Redis 要服务多页，就必须认真处理 `latest_time + latest_id`。

##### 5.1 建议模块结构

不要让 `FeedService` 直接到处调用 `redis.Client.ZAdd/ZRevRange...`。可以在 `internal/feed` 内增加一个小缓存接口，底层仍然复用 `internal/cache.Client`：

```text
internal/feed/
  service.go
  handler.go
  latest_cache.go        -> FeedLatestCache 接口和 RedisFeedLatestCache 实现
```

接口可以先保持很小：

```go
type FeedLatestCache interface {
    ListLatestIDs(ctx context.Context, latestTime time.Time, latestID uint, limit int) ([]LatestItem, bool, error)
    AddVideo(ctx context.Context, videoID uint, createdAt time.Time) error
    RemoveVideo(ctx context.Context, videoID uint) error
}

type LatestItem struct {
    VideoID   uint
    CreatedAt int64
}
```

含义：

- `ListLatestIDs`：从 `myfeed:feed:latest` 中取一页视频 ID。`bool` 表示是否使用了缓存，Redis disabled、key 不存在、缓存数量不足时可以返回 `used=false`，让 service 直接回源 MySQL。
- `AddVideo`：视频发布成功后，把 `video_id` 写入 ZSET。
- `RemoveVideo`：视频删除、下架、状态变更时，从 ZSET 中移除。

##### 5.2 读流程

`FeedService.ListLatest` 的主流程建议是：

```text
1. 校验 limit、latest_time、latest_id。
2. 尝试 feedLatestCache.ListLatestIDs(ctx, latestTime, latestID, limit+1)。
3. Redis disabled / miss / error / 返回数量不足：
   -> 直接走当前 MySQL repo.ListLatest(limit+1, latestTime, latestID)。
4. Redis 命中：
   -> 拿到 video_id 列表。
   -> 优先批量读取 video:detail 缓存。
   -> 详情 miss 的 video_id 批量回源 MySQL。
   -> 按 Redis 返回的 video_id 顺序重新组装结果。
5. 仍然用 limit+1 判断 has_more。
6. next_time、next_id 仍然取返回页最后一个视频的 created_at 和 id。
```

这里有几个细节：

- Redis ZSET 只保存 ID，不保存完整 DTO。
- 批量回源 MySQL 后，`WHERE id IN (...)` 不保证返回顺序，必须用 `map[id]VideoDTO` 按 Redis ID 顺序重排。
- 如果某个 ID 在 Redis 中存在，但 MySQL 查不到，或者视频 `status != 1`，说明 ZSET 里有脏数据。第一版可以跳过它，并 best-effort `ZREM`。
- 如果脏数据导致本页凑不够 `limit`，不要硬凑复杂逻辑；第一版可以直接回源 MySQL，保证接口语义正确。

##### 5.3 双游标在 ZSET 中怎么查

第一页：

```text
ZREVRANGE myfeed:feed:latest 0 limit WITHSCORES
```

返回的是 score 从大到小，也就是 `created_at` 新到旧。同 score 时，固定宽度 member 的反向字典序可以对应 `id DESC`。

下一页要表达的条件仍然是：

```text
created_at < latest_time
OR (created_at = latest_time AND id < latest_id)
```

ZSET 里可以拆成两段查：

```text
1. 查 score == latest_time 的同毫秒数据，过滤出 video_id < latest_id。
2. 如果还不够 limit+1，再查 score < latest_time 的数据。
3. 合并两段结果，截断到 limit+1。
```

不要只写：

```text
ZREVRANGEBYSCORE key (latest_time -inf
```

这样会漏掉同一毫秒内 `id < latest_id` 的视频，和 MySQL 的复合游标语义不一致。

如果你觉得两段查询太麻烦，可以先让 Redis 只承接第一页：

```text
latest_time == 0 && latest_id == 0 -> 尝试 Redis
其他分页请求 -> 走 MySQL
```

这个版本收益没那么大，但语义更稳，适合作为第一步。

##### 5.4 写流程

视频发布流程应保持 MySQL 为主：

```text
VideoService.Publish
  -> MySQL 事务创建 videos/tags/video_tags
  -> 事务成功后，AddVideo 写入 myfeed:feed:latest
  -> Redis 写失败只记录日志，不回滚视频发布
```

`AddVideo` 内部大致做：

```text
ZADD myfeed:feed:latest created_at_milli padded_video_id
ZREMRANGEBYRANK myfeed:feed:latest 0 -(max_size+1)   // 只保留最新 N 条
EXPIRE myfeed:feed:latest 10m                         // 可选，用于学习阶段自恢复
```

建议第一版 `max_size` 可以设成 `1000` 或 `5000`。Redis 只保存热点最新流，不保存全量历史。用户翻到很旧的数据时，直接回源 MySQL。

为什么写 Redis 失败不能回滚 MySQL：

- Redis 是缓存和加速层，不是主存储。
- 视频已经发布成功时，MySQL 里必须有完整数据。
- Redis 写失败最多导致新视频暂时不出现在缓存首页，后续可以通过 MySQL 回源、定时重建、启动预热或 TTL 过期修复。

如果后面实现视频删除、下架、状态变更，需要在 MySQL 成功后调用 `RemoveVideo`，把对应 ID 从 `myfeed:feed:latest` 移除。

##### 5.5 冷启动和重建

Redis 第一次启动、key 过期、手动清空缓存时，`myfeed:feed:latest` 可能不存在。不要让接口失败。

第一版可以这样处理：

```text
ListLatest 发现 Redis key 不存在
  -> 回源 MySQL 查询当前页
  -> 如果是第一页，顺手把 MySQL 最新 N 条重建到 ZSET
```

也可以先不做自动重建，只要保证 Redis miss 时回源 MySQL。等读链路稳定后，再补：

```go
RebuildLatest(ctx context.Context, videos []video.Video) error
```

##### 5.6 需要特别注意的坑

这部分和前面 token、video detail 缓存已经踩过的坑类似，后续实现 Feed 缓存时要提前避开：

- `redis.Nil` 是缓存 miss，不是业务错误；Redis disabled 是主动跳过缓存，也不要打印成 `failed`。
- Redis 读失败时，`listLatest` 应回源 MySQL；MySQL 失败才是接口失败。
- 视频发布成功后的 `AddVideo` 属于 DB 提交后的缓存维护。它失败不能回滚 MySQL，也不要让发布接口因为 Redis 临时失败直接 500。
- DB 事务成功后的缓存维护动作，建议使用 `context.Background() + context.WithTimeout` 创建短超时，不要完全依赖请求 `ctx`，避免客户端断开后缓存没有维护。
- 写日志要区分 `skip cache because redis disabled`、`cache miss`、`cache error`，不要把跳过缓存写成成功，也不要把 disabled 写成失败。
- 继续保持 `latest_time + latest_id` 双游标，不要因为换成 Redis 就退回只按时间戳分页。

##### 5.7 验收标准

实现完成后至少满足：

1. Redis 关闭时，`/feed/listLatest` 仍然走 MySQL 正常返回。
2. Redis 开启且 key 为空时，第一页能回源 MySQL。
3. Redis 命中时，返回顺序和 MySQL 的 `created_at DESC, id DESC` 一致。
4. 同一毫秒内多个视频不会因为分页漏掉或重复。
5. 发布新视频后，第一页能在 Redis 中读到新 ID；Redis 写失败时，发布仍成功并记录日志。
6. Redis 中有下架或不存在的视频 ID 时，接口不应返回脏视频。

注意：

- 冷数据可以回源 MySQL，不要强求 Redis 保存全部历史。
- Feed ZSET 的一致性边界要能讲清楚：Redis 写失败时可能短暂少展示新视频，但 MySQL 数据不丢，缓存可重建。

#### 6. 热榜

可以使用 Redis ZSET：

```text
key:   myfeed:rank:hot
score: popularity
value: video_id
```

第一版热度公式不要复杂：

```text
popularity = likes_count * 3 + comments_count * 5
```

更新策略：

```text
点赞成功 -> 更新视频 likes_count -> 更新热榜 score
取消点赞 -> 更新视频 likes_count -> 更新热榜 score
评论成功 -> 更新视频 comments_count -> 更新热榜 score
删除评论 -> 更新视频 comments_count -> 更新热榜 score
```

V1.0 先同步更新 Redis 热榜即可。V1.5 再考虑 MQ + Worker 异步更新。

#### 7. 基础限流

可以先做固定窗口：

```text
INCR myfeed:rate:{scene}:{identifier}
EXPIRE key window_seconds
```

适合场景：

- 登录失败限流。
- 上传接口限流。
- 发布视频限流。
- 评论/私信限流。
- 点赞/取消点赞限流。

注意：

- 限流失败返回 `429 Too Many Requests`。
- key 要区分 IP、用户 ID、业务场景。
- 登录前没有用户 ID，只能用 IP 或用户名维度。
- 这一版固定窗口就够了，不要过早实现滑动窗口或令牌桶。

#### 8. 分片上传，后置可选

分片上传仍然属于 V1.0 可扩展项，但不要排在前面。先把缓存、Feed、热榜、限流跑稳，再做分片上传。

可选接口：

```text
POST /video/chunk/init
POST /video/chunk/upload
POST /video/chunk/status
POST /video/chunk/complete
```

Redis 可保存上传会话：

```text
myfeed:chunk_upload:{upload_id}              -> 分片上传会话 JSON
myfeed:chunk_upload_hash:{account_id}:{hash} -> upload_id
```

### 难点预警

- 缓存不是只写 `GET/SET`。你要处理缓存穿透、击穿、雪崩和主动失效。
- 视频详情缓存里如果包含点赞数，点赞后要么主动删除缓存，要么接受短暂不一致。
- 用户主页聚合缓存一定会有短暂不一致，必须通过 TTL 或主动失效控制影响范围。
- Feed ZSET 和 MySQL 双游标要保持排序语义一致，否则 Redis 页和 DB 页会出现重复或漏数据。
- 热榜分页不能直接读实时变化的 ZSET 后假装稳定分页；如果要严格分页，需要做 `as_of` 快照。
- 分片上传必须校验 `chunk_hash`，否则合并出来的视频可能损坏。
- Redis 不可用时，核心业务必须能退回 MySQL，只是变慢，不能直接挂掉。
- 不要在 V1.0 就把点赞、评论写成异步。同步链路正确后，V1.5 再引入 MQ。

### V1.0 面试亮点

这一阶段是简历上开始有分量的地方，但前提是你真的能讲清楚：

- 为什么不用 offset，而用 `created_at + id` 复合游标。
- Redis 缓存如何设计 key、TTL、失效策略。
- Cache Aside 的一致性边界。
- Redis 挂了系统怎么降级。
- 点赞/评论计数字段为什么是冗余字段，如何保证与明细表一致。
- Feed 最新流为什么适合 ZSET。
- 热榜如何计算、如何更新、如何避免每次全表排序。
- 限流为什么要按场景设计 key。
- 为什么 V1.0 先做 Redis，同步链路稳定后 V1.5 再做 MQ。

严厉一点说：V1.0 开始后，每加一个 Redis key，都必须能回答三个问题：

```text
它缓存什么？
什么时候失效？
Redis 挂了怎么办？
```

---

## V1.5：RabbitMQ + Worker + Outbox + SSE 通知

### 阶段目标

把同步写压力拆出去，形成真正的事件驱动架构：

- 点赞异步落库
- 评论异步落库
- 关注异步落库
- 热度异步更新
- 视频发布时间线 Outbox
- 通知异步生成
- SSE 实时推送

这一步是简历重点，但也是最容易写烂的一步。你要先保证 V0.3 的同步版本正确，再抽成异步。

### 技术栈引入

- `RabbitMQ`：削峰、解耦、异步处理。
- 独立 `cmd/worker`：消费消息。
- Outbox Pattern：解决“DB 写成功但 MQ 投递失败”的一致性问题。
- SSE：实现轻量实时通知。

### MySQL 表补充

```sql
notifications
- id bigint primary key
- account_id bigint not null
- type varchar(32) not null
- actor_id bigint
- video_id bigint
- comment_id bigint
- content varchar(500)
- is_read boolean default false
- created_at datetime

outbox_msgs
- id bigint primary key
- event_id varchar(64) unique not null
- topic varchar(128) not null
- payload text not null
- status tinyint default 0
- retry_count int default 0
- created_at datetime
- updated_at datetime
```

### RabbitMQ 事件设计

```text
like.events
- like.like
- like.unlike

comment.events
- comment.publish
- comment.delete

social.events
- social.follow
- social.unfollow

video.popularity.events
- video.popularity.update

video.timeline.events
- video.timeline.publish
```

### 开发顺序建议

1. 封装 RabbitMQ 连接、exchange、queue、binding。
2. 新建 `cmd/worker/main.go`。
3. 先只异步化点赞：API 投递 `like.like`，Worker 消费并写 MySQL。
4. 给点赞 Worker 加幂等逻辑，重复消息不能重复加计数。
5. 加 MQ 失败降级：投递失败时走 V0.3 的同步写逻辑。
6. 异步化评论和关注。
7. 写 `PopularityWorker`，消费热度事件并更新 Redis 热榜。
8. 发布视频时，事务内同时写 `videos` 和 `outbox_msgs`。
9. 写 `OutboxPoller`，扫描未投递消息，投递到 `video.timeline.events`。
10. 写 `TimelineConsumer`，消费后写 `feed:global_timeline`。
11. 写通知表和通知服务。
12. 写 `GET /notification/stream?token=xxx` SSE 长连接。
13. 写 `POST /notification/list`、`markRead`、`unreadCount`。

### 难点预警

- MQ 消息一定可能重复消费，所以 Worker 必须幂等。
- API 返回成功不代表 Worker 已经落库，前端可能看到短暂延迟，这是最终一致性。
- Outbox 是为了保证“视频记录”和“发布时间线事件”不分家。
- SSE 连接要处理断开、心跳、用户多端连接和 channel 阻塞。
- 不要把所有业务一口气异步化。先点赞，再评论，再关注，逐个验证。

---

## V2.0：微服务拆分，可选体验版

### 阶段目标

这是锦上添花，不是必须。你可以把单体按领域拆成：

- `account-service`
- `video-service`
- `interaction-service`
- `feed-service`
- `notification-service`
- `api-gateway`

### 技术栈引入

- `gRPC`：服务间通信。
- `protobuf`：接口契约。
- Docker Compose：多服务本地编排。
- 可选服务注册：先不用引入复杂注册中心。

### 拆分建议

先拆 `feed-service` 或 `notification-service`，不要先拆账号。账号是基础依赖，先拆它会让你到处改鉴权。

### 难点预警

- 微服务不是简历加分项本身，能讲清楚边界、数据归属、调用链和故障降级才加分。
- 如果你连单体事务都没写明白，微服务只会放大问题。
- 简历里可以写“预留 gRPC 拆分方案”，不一定非要完整落地。

## 3. 学习与落地建议

## 面试官最看重的亮点

你要重点打磨这些地方：

1. Feed 分页设计：`created_at + id`、`likes_count + id`、热榜 `as_of + offset`，讲清楚为什么不用简单 offset。
2. Redis 缓存策略：视频详情缓存、Feed 时间线、热榜 ZSET、缓存击穿保护、主动失效。
3. 点赞/评论/关注一致性：唯一索引、事务、幂等、计数字段维护。
4. RabbitMQ 异步化：API 快速返回、Worker 消费、失败降级、重复消息处理。
5. Outbox Pattern：解决本地事务和 MQ 投递不一致。
6. 分片上传：文件 hash、chunk hash、断点续传、合并校验。
7. SSE 通知：在线推送 + 离线通知表。
8. 工程质量：清晰分层、配置管理、日志、健康检查、Docker Compose。

## 推荐 Go 开源库

```text
Web 框架
- github.com/gin-gonic/gin

ORM
- gorm.io/gorm
- gorm.io/driver/mysql

Redis
- github.com/redis/go-redis/v9

RabbitMQ
- github.com/rabbitmq/amqp091-go

JWT
- github.com/golang-jwt/jwt/v5

密码哈希
- golang.org/x/crypto/bcrypt

配置
- github.com/spf13/viper
- github.com/joho/godotenv

日志
- go.uber.org/zap
- github.com/rs/zerolog

参数校验
- github.com/go-playground/validator/v10

雪花 ID
- github.com/bwmarrin/snowflake

测试
- github.com/stretchr/testify
- github.com/DATA-DOG/go-sqlmock

接口文档
- github.com/swaggo/swag
- github.com/swaggo/gin-swagger
```

最后给你一个严格建议：每个版本都要能独立运行、能用 Postman 跑通、能写进 README。不要边写边想着“以后再补”。这个项目的价值不在于功能数量，而在于你能把一个短视频系统从单体 CRUD 讲到缓存、异步、最终一致性和高并发读写优化。
